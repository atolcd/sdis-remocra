// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package fr.sdis83.remocra.domain.remocra;

import fr.sdis83.remocra.domain.remocra.ZoneSpeciale;
import javax.persistence.EntityManager;
import javax.persistence.TypedQuery;

privileged aspect ZoneSpeciale_Roo_Finder {
    
    public static Long ZoneSpeciale.countFindZoneSpecialesByCode(String code) {
        if (code == null || code.length() == 0) throw new IllegalArgumentException("The code argument is required");
        EntityManager em = ZoneSpeciale.entityManager();
        TypedQuery q = em.createQuery("SELECT COUNT(o) FROM ZoneSpeciale AS o WHERE o.code = :code", Long.class);
        q.setParameter("code", code);
        return ((Long) q.getSingleResult());
    }
    
    public static TypedQuery<ZoneSpeciale> ZoneSpeciale.findZoneSpecialesByCode(String code) {
        if (code == null || code.length() == 0) throw new IllegalArgumentException("The code argument is required");
        EntityManager em = ZoneSpeciale.entityManager();
        TypedQuery<ZoneSpeciale> q = em.createQuery("SELECT o FROM ZoneSpeciale AS o WHERE o.code = :code", ZoneSpeciale.class);
        q.setParameter("code", code);
        return q;
    }
    
    public static TypedQuery<ZoneSpeciale> ZoneSpeciale.findZoneSpecialesByCode(String code, String sortFieldName, String sortOrder) {
        if (code == null || code.length() == 0) throw new IllegalArgumentException("The code argument is required");
        EntityManager em = ZoneSpeciale.entityManager();
        String jpaQuery = "SELECT o FROM ZoneSpeciale AS o WHERE o.code = :code";
        if (fieldNames4OrderClauseFilter.contains(sortFieldName)) {
            jpaQuery = jpaQuery + " ORDER BY " + sortFieldName;
            if ("ASC".equalsIgnoreCase(sortOrder) || "DESC".equalsIgnoreCase(sortOrder)) {
                jpaQuery = jpaQuery + " " + sortOrder;
            }
        }
        TypedQuery<ZoneSpeciale> q = em.createQuery(jpaQuery, ZoneSpeciale.class);
        q.setParameter("code", code);
        return q;
    }
    
}
