// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package fr.sdis83.remocra.domain.remocra;

import fr.sdis83.remocra.domain.remocra.TypeDroit;
import javax.persistence.EntityManager;
import javax.persistence.TypedQuery;

privileged aspect TypeDroit_Roo_Finder {
    
    public static Long TypeDroit.countFindTypeDroitsByCode(String code) {
        if (code == null || code.length() == 0) throw new IllegalArgumentException("The code argument is required");
        EntityManager em = TypeDroit.entityManager();
        TypedQuery q = em.createQuery("SELECT COUNT(o) FROM TypeDroit AS o WHERE o.code = :code", Long.class);
        q.setParameter("code", code);
        return ((Long) q.getSingleResult());
    }
    
    public static TypedQuery<TypeDroit> TypeDroit.findTypeDroitsByCode(String code) {
        if (code == null || code.length() == 0) throw new IllegalArgumentException("The code argument is required");
        EntityManager em = TypeDroit.entityManager();
        TypedQuery<TypeDroit> q = em.createQuery("SELECT o FROM TypeDroit AS o WHERE o.code = :code", TypeDroit.class);
        q.setParameter("code", code);
        return q;
    }
    
    public static TypedQuery<TypeDroit> TypeDroit.findTypeDroitsByCode(String code, String sortFieldName, String sortOrder) {
        if (code == null || code.length() == 0) throw new IllegalArgumentException("The code argument is required");
        EntityManager em = TypeDroit.entityManager();
        String jpaQuery = "SELECT o FROM TypeDroit AS o WHERE o.code = :code";
        if (fieldNames4OrderClauseFilter.contains(sortFieldName)) {
            jpaQuery = jpaQuery + " ORDER BY " + sortFieldName;
            if ("ASC".equalsIgnoreCase(sortOrder) || "DESC".equalsIgnoreCase(sortOrder)) {
                jpaQuery = jpaQuery + " " + sortOrder;
            }
        }
        TypedQuery<TypeDroit> q = em.createQuery(jpaQuery, TypeDroit.class);
        q.setParameter("code", code);
        return q;
    }
    
}
